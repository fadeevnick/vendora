# Execution Layer

This layer is where Vendora is realized and verified.

## Current Transitional Rule

The imported codebase remains outside this folder:

- `../vendora_codebase/`

It is treated as the current execution asset.

## Rules

- code does not replace product artifacts as source of truth;
- runtime claims must be backed by actual execution;
- implementation tracking should reflect verified reality only.

## Runtime Docs

Runtime depth and phase-specific verification rules live in:

- `runtime/`
